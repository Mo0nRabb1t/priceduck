# 代码复查报告 — 计价鸭 V1.1（2026-07-18）

复查范围：`E:\sourcecode\aicode` 当前代码，对照 V1.1 变更任务书（移除语音输入）+ 用户最新要求（应用名「计价鸭」、占位 Logo）。

---

## ✅ 1. 布局修复（任务书 T-5 方案 X）—— 已正确

`lib/pages/record/record_page.dart` 已改为：
- 去掉包裹 `RecordForm` 的 `SingleChildScrollView`；
- `RecordForm()` 直接作为 `Column` 子节点（页首固定）；
- `RecordList()` 置于 `Expanded` 内，独占剩余空间、内部滚动。

→ 符合方案 X（表单内联、列表扩容），原「列表被压没」隐患消除。

---

## ❌ 2. 应用名「计价鸭」—— 代码未生效，仍显示「物价记录助手」

代码中显示名称仍是旧名，需改以下 5 处（包名可保留，见下）：

| 文件 | 行 | 当前 | 应改为 |
|------|----|------|--------|
| `lib/main.dart` | 22 | `title: '物价记录助手'` | `'计价鸭'` |
| `android/app/src/main/AndroidManifest.xml` | 3 | `android:label="物价记录助手"` | `"计价鸭"` |
| `ios/Runner/Info.plist` | 8 | `<string>物价记录助手</string>` (CFBundleDisplayName) | `<string>计价鸭</string>` |
| `pubspec.yaml` | 2 | `description: "物价记录助手 - 离线比价 App"` | 建议改为含「计价鸭」 |
| `README.md` | 1 | `# Priceduck - 物价记录助手` | 同步 |

> 注：技术包名 `priceduck` / `com.priceduck.assistant`（build.gradle、MainActivity.kt、Info.plist:16 CFBundleName）**建议保留**——改包名要动目录结构与 Kotlin 包声明，属无收益改动。用户本次只要求改显示名「计价鸭」。

---

## ❌ 3. 占位 Logo 引用 —— 实际未接入，且路径在项目的外部（重点）

**用户认知**：Logo 已「引用」`E:\sourcecode\docfordev\priceduck.jpeg` 作占位。
**实际事实**：该图片**根本没有接进 App**。

核实结果：
- 文件确实存在于 `E:\sourcecode\docfordev\priceduck.jpeg`（795 KB，2024-12-06）；
- 但 `lib/` 内**无任何 `Image` 组件**，`pubspec.yaml` **无 `flutter: assets:` 段**，`assets/` 目录为空；
- 该路径位于**项目目录 `E:\sourcecode\aicode` 之外**。Flutter 无法引用项目外的绝对路径；即便用 `Image.file('E:/sourcecode/docfordev/priceduck.jpeg')`，也仅桌面调试可跑，真机/打包必然失败；
- `README.md:63` 仅是一句「未来用 `flutter_launcher_icons` 生成图标」的说明文字，**不是接入代码**。

→ 纠正：当前 App 既不显示该 Logo，也无法显示（路径外部 + 未声明资产）。

**标准接入方式（让占位图真正可见）：**
1. 复制文件进项目：`aicode/assets/icons/priceduck.jpeg`；
2. `pubspec.yaml` 增加：
   ```yaml
   flutter:
     assets:
       - assets/icons/priceduck.jpeg
   ```
3. 在目标位置用 `Image.asset('assets/icons/priceduck.jpeg')`；
4. 若需手机桌面启动图标，再用 `flutter_launcher_icons` 处理（需把图放到其配置指向的路径）。

由于用户声明「现在先不用确定、后续换正式设计」，建议至少先把占位图按上述方式接进项目（哪怕只接一处），并留 TODO 注释标明「占位，待替换」，避免后续找不到接入点。

---

## ⚠️ 4. Git 状态提醒（次要）

`git status` 显示项目内所有文件均为**未跟踪（`??`）**，未见实际 commit 记录。若你认为代码「已提交」，请确认仓库是否真正 `commit`/`push`，避免改动丢失或无法回溯。

---

## 待用户确认

- **A. 应用名**：是否要我直接把上述 5 处「物价记录助手」改为「计价鸭」？（包名 `priceduck` 保留）
- **B. 占位 Logo 显示位置**：
  - (a) 仅手机桌面启动图标 → `flutter_launcher_icons` 处理；
  - (b) App 内某处展示（如记录页标题栏 / 关于页）→ assets 接入 + `Image.asset`；
  - (c) 两者都要；
  - 或 (d) 先不动，等正式设计一次性接入。
- **C. Git**：是否需要提醒开发确认已真正提交？
